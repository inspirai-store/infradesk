apiVersion: v1
kind: Secret
metadata:
  name: zeni-x-secrets
type: Opaque
stringData:
  # 这是示例模板，请复制到各环境 overlays 下的 secrets.yaml，并替换为真实值
  # 例如: cp k8s/secrets/secrets.example.yaml k8s/overlays/dev/secrets.yaml
  MYSQL_ROOT_PASSWORD: "CHANGE_ME"
  REDIS_PASSWORD: "CHANGE_ME"


