Thank you for installing Zeni-X!

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }} -n {{ .Release.Namespace }}
  $ helm get all {{ .Release.Name }} -n {{ .Release.Namespace }}

{{- if .Values.ingress.enabled }}

{{- range $host := .Values.ingress.hosts }}
Frontend URL: http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}
{{- end }}

{{- range $host := .Values.ingress.hosts }}
API URL: http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}/api
{{- end }}
{{- else }}

Frontend will be available via port-forward:

  $ kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "zeni-x.fullname" . }} 8080:80

Then access the application at: http://localhost:8080
{{- end }}

To check the status of your pods:

  $ kubectl get pods -n {{ .Release.Namespace }}

To view the logs:

  $ kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "zeni-x.name" . }},app.kubernetes.io/instance={{ .Release.Name }}

To uninstall the chart:

  $ helm uninstall {{ .Release.Name }} -n {{ .Release.Namespace }}
